function setup() {
  createCanvas(windowWidth, windowHeight);
  background(255, 0, 200);
}
function draw() {
  //let r = random(0,255)
  if (mouseIsPressed) {
    circle(100,100,100);
  }
  else{
   }
}
